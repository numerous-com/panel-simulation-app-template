name = "Simulation App"
description = "This is a template for a simulation app."
port = 80
cover_image = "app_cover.png"
exclude = [ "*venv", "venv*", ".git", ".env" ]

[python]
library = "panel"
app_file = "app.py"
requirements_file = "requirements.txt"
version = "3.12"

[deploy]
app = "simulation-app"